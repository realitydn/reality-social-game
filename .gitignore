# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (do not commit)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
# cloudflare-env.d.ts is committed; regenerate via `npm run cf-typegen` after editing wrangler.jsonc

# cloudflare / opennext
.open-next/
.wrangler/
.dev.vars

# editor
.vscode/
.idea/

# claude code local state (worktrees, session data). Commit specific config
# with `git add -f .claude/<file>` if you ever want to share it.
.claude/
